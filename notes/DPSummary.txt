DP 

CLRS Chapter 16
Two Elements of DP:
1. Optimal substructure      => The best answer
2. Overalapping subproblmes   => infer f(n) from f(n-1)

Two types of DP:

1. Bottom up   Tabular Solution  pros: constant factor faster than top down cons:  need to compute the whole table
2. Top down recursive + Memorization  Cons: overhead of recursion pros: compute only when neccessary (good for sparse matrix/table)

Finding out the solution:
Can use another table to track the breaking point so can backtrace the solution space...... Ch. 16.1 e.g., table s and Matrix-Chain-MultiplY


Two Dimensional DP
for(int i=0;i<length-1;i++)}{


1. Parser tree -- add parenthesis to maximize overall value
2. break strings to words
3. matrix chain multiplication fewest operation
4. Palindrome partition
